# تعديلات v6 — الكورة برجل اللاعب + تصليح سبرايتات اللعيبة

## 1) اللاعب بيزق الكورة برجله
- كلاس جديد `Dribble` في `Engine.kt` فيه حالة الكورة: `lead` (بعدها الأفقي عن نص اللاعب)، `y` (ارتفاعها عن الأرض)، `vy`، `vLead`، `spin`، `spinVel`.
- `stepDribble(f, spd)` بتتنادى من لوب اللعب ومن لوب المنيو، فالكورة شغالة في الشاشتين.
- على الأرض: كل ما دورة الخطوات تكمل نص لفة (`frames / 2`) الرجل بتزق الكورة → دفعة أفقية `KICK_PUSH + spd * 0.045` + رفعة صغيرة `KICK_HOP` + تراب صغير مكان اللمسة.
- الكورة مربوطة قدام الرجل بسستة (spring) مع damping، ومحصورة بين `rest * 0.55` و `rest * 1.95`، فمش ملزوقة في مكان ثابت ومش بتسيب اللاعب.

## 2) تنطيط الكورة بالرجل وهو طاير
- وهو في الهوا: لما الكورة تنزل لمستوى الجزمة (`footY + br * 1.2`) وهي نازلة، الرجل بتنططها لفوق بـ `JUGGLE_UP + footY * 0.035` مع تغيير اتجاه اللف، وفي cooldown صغير يمنع الرجفة.
- الكورة ليها جاذبية خاصة بيها `BALL_GRAV = 0.62` (أخف من جاذبية اللاعب) ونطة على الأرض بمعامل ارتداد 0.42.

## 3) لف الكورة بقى واقعي
- قبل كده كان اللف = `st.dist / br` (المسافة كلها من أول الجراية) → حوالي 52 درجة في الفريم ≈ 3100 درجة/ثانية، بيبان زي الفرَّاية.
- بقى: معدل تدحرج حقيقي `spd / br` معدَّل بـ 0.28، مهدَّى بـ smoothing ومحدود بسقف `SPIN_MAX = 13` درجة/فريم.

## 4) تصليح سبرايتات اللعيبة (20 لاعب)
- الأصل: مساحات من الشورت/الفخد مقصوصة بالكامل (alpha = 0) في ملفات الـ WebP، والـ RGB تحتها ضايع فمفيش استرجاع للبكسل الأصلي.
- الحل: `sprite_doctor.py` بيكتشف الفتحات بالمكونات المتصلة، وبيملّاها بلون الكيت نفسه (median لون القميص بعد استبعاد لون الجلد) بتظليل رأسي وحدود مموّهة، وكمان بيصلّح البكسلات نصف الشفافة حوالين الفتحة. كل حاجة اتحفظت WebP lossless.
- اللي اتصلح: aboutrika, zidane, baggio, mbappe, messi, cruyff, kaka, emartinez, osimhen, yamal, dimaria, musiala, robertocarlos, totti, gerdmuller, bounou, ibrahimovic, platini, drogba, gullit.
- الـ 81 لاعب السليم رجعوا من النسخة الأصلية بايت بايت — مفيش أي تعديل عليهم.

## 5) ميسي
- كان بيطير فوق الأرض (فراغ 10–18 بكسل تحت رجله في كل الفريمات) وخليّته قاعد على الأرض بعد إعادة قصّ الفريم.
- مقاس الفريم اتغيّر في `Players.kt` من `131 × 176` لـ `88 × 159`.

## 6) مقاسات فريمات كانت غلط في الكود
| اللاعب | قبل | بعد |
| --- | --- | --- |
| messi | 131 × 176 | 88 × 159 |
| bounou | 104 × 176 | 100 × 169 |
| emartinez | 105 × 176 | 100 × 168 |
| neuer | 103 × 176 | 100 × 171 |

التحقق: كل 101 لاعب مقاس صورته = `frames × fw` و `fh` زي اللي في `Players.kt` بالظبط.
